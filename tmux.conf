# Optional, portable defaults; no plugins or automatic commands.
set -g history-limit 10000
set -g mouse on
setw -g mode-keys vi
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -g status-left '[#S] '
set -g status-right '%H:%M'
